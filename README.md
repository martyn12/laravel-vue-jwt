Небольшое приложение на Laravel для освоения функционала авторизации в SPA-приложениях с помощью JWT.

Запуск:

```
composer install
cp .env.example .env
npm install
php artisan serve
npm run dev
```
